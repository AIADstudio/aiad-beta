diff --git a/index.html b/index.html
index 5bb2d39..1c5bfe6 100644
--- a/index.html
+++ b/index.html
@@ -579,6 +579,14 @@ html, body {
 .live-stat-item .stat-change.down { color: #FF6B6B; background: rgba(255,107,107,0.1); }
 .live-stat-item .stat-change.new  { color: var(--teal-light); background: rgba(0,212,255,0.1); }
 </style>
+<style id="aiad-sidebar-footer-fix">
+/* Keep the sidebar account row (Settings / Sign out) reachable on desktop: bound the
+   sidebar to the viewport height, let the nav scroll, and pin the footer to the bottom. */
+@media (min-width:769px){
+  .dash-sidebar-left{height:100vh !important;max-height:100vh !important;overflow-y:auto !important;position:sticky !important;top:0 !important;}
+  .dash-sidebar-left .dash-sidebar-footer{position:sticky !important;bottom:0 !important;background:var(--bg-secondary);}
+}
+</style>
 </head>
 <body>
 <!-- Records the most recent error so the bug-report form can attach it. Sits here
